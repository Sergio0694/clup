namespace clup.Enums
{
    /// <summary>
    /// An enum that indicates the type of message being displayed to the user
    /// </summary>
    internal enum MessageType
    {
        Info,
        Error
    }
}